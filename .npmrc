# Lockfile version
package-lock=true

# Save exact versions
save-exact=true

# Engine strict (require Node >= 18)
engine-strict=true

# Audit signatures
audit-level=moderate

# Enable provenance (only works in GitHub Actions CI)
# provenance=true
